import React from 'react';
import {Col, Container, Row} from "react-bootstrap";
import "./about.css"

const Education = React.forwardRef((props, ref) => {

    return (
        <Container className="w-100" ref={ref}>
            <Row>
                <Col>

                </Col>
            </Row>
        </Container>
    );
});
export default Education;